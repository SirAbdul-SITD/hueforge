import 'package:flutter/material.dart';

/// Neutral graphite studio theme, deliberately understated -- the app
/// itself is about color, so the chrome shouldn't compete with whatever
/// color the user is working with.
class Palette {
  static const void_ = Color(0xFF15161A);
  static const panel = Color(0xFF1D1F24);
  static const raised = Color(0xFF272A31);

  static const ink = Color(0xFFEEF0F3);
  static const haze = Color(0xFF888E9A);
  static const line = Color(0xFF2C303A);

  static const accent = Color(0xFFB98CE0);
  static const good = Color(0xFF6FA37B);
  static const warn = Color(0xFFCFA24C);
  static const bad = Color(0xFFD1685A);
}
